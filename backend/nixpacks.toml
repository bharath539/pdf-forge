[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3.11 -m ensurepip", "python3.11 -m pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python3.11 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
